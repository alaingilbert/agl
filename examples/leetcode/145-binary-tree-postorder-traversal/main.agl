package main

type TreeNode struct {
    Val int
    Left *TreeNode
    Right *TreeNode
}

func postorderTraversal(root *TreeNode) []int {
    mut out := []int{}
    if root == nil {
        return out
    }
    if root.Left != nil {
        out.Push(postorderTraversal(root.Left)...)
    }
    if root.Right != nil {
        out.Push(postorderTraversal(root.Right)...)
    }
    out.Push(root.Val)
    return out
}

func main() {
}