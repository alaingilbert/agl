package main

func construct2DArray(original []int, m int, n int) [][]int {
    mut out := [][]int{}
    if m * n != original.Len() {
        return out
    }
    for i := 0; i < m; i++ {
        out.Push(original[i*n:i*n+n])
    }
    return out
}

func main() {
    // construct2DArray([]int{1,2,3,4}, 2, 2) // [[1,2],[3,4]]
}