package main

func findPeaks(mountain []int) []int {
    Array((1..mountain.Len()-1).
        Filter(|i| mountain[i-1] < mountain[i] && mountain[i] > mountain[i+1]))
}

func main() {
    print(findPeaks([]int{1,4,3,8,5}))
}