package main

func findPeaks(mountain []int) []int {
    mountain.
        Enumerated().
        DropFirst(1).
        DropLast(1).
        FilterMap(|(i, e)| if mountain[i-1] < e && e > mountain[i+1] { Some(i) } else { None })
}

func main() {
    print(findPeaks([]int{1,4,3,8,5}))
}